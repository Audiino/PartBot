import { GamesList } from '@/ps/games/common';

import type { Meta } from '@/ps/games/common';

export const meta: Meta = {
    name: 'Guess Number',
    id: GamesList.GuessNumber,
    aliases: ['gn'],
    abbr: 'gn',
    players: 'single',
};
